// Optional: show a message after successful submission
document.addEventListener("DOMContentLoaded", () => {
  const status = document.getElementById('status');
  if (status) {
    // Netlify will redirect back to the same page by default
    // You can use a thank-you page later if you like.
  }
});
